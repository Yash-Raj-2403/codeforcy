#include<bits/stdc++.h>using namespace std; int main(){    int t;    cin >> t;    while(t--)    {        int n;        cin >> n;        vector<int> a(n);        int min = INT_MAX;        for(int i=0;i<n;i++)        {            cin >> a[i];            if(a[i]< min)            {                min = a[i];            }        }        bool tmkc = false;        int prod =1;        for(int i=0;i<n;i++)        {            if(min == a[i] && !tmkc)            {                prod = prod * (a[i]+1);                tmkc = true;            }            else            {                prod = prod * a[i];            }        }        cout << prod << endl;    }    }