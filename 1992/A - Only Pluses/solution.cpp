#include <bits/stdc++.h>using namespace std; int main() {	// your code goes here    int t;    cin >> t;    while(t--)    {            int n=3;        vector<int> a(n);        for(int i=0;i<n;i++)        {            cin >> a[i];        }        sort(a.begin(),a.end());        int dick =5;        while(dick--)        {            a[0] = a[0]+1;            sort(a.begin(),a.end());        }        int prod =1;        for(int i=0;i<n;i++)        {            prod = prod *a[i];        }        cout << prod << endl;    }}